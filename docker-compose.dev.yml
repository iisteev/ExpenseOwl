services:
  expenseowl:
    build:
      context: .
    develop:
      watch:
        - path: ./internal
          action: rebuild
        - path: ./cmd
          action: rebuild
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - data:/app/data

volumes:
  data:
